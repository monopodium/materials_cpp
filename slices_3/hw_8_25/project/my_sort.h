#ifndef MY_SORT_H
#define MY_SORT_H
#include "my_vector.h"
#include "my_list.h"
#include <algorithm>

void my_sort(my_vector<int> &vec){
    std::cout<<"sort my_vector"<<std::endl;
    // 补全，将my_vector中的元素排序
};
void my_sort(my_list<int> &lst)
{
    std::cout<<"sort my_list"<<std::endl;
    // 补全，将my_list中的元素排序
}

#endif